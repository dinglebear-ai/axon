interface:
  display_name: "Axon CLI"
  short_description: "Route web search, source indexing, extraction, RAG, jobs, and watch tasks through Axon."
  brand_color: "#29B6F6"
policy:
  allow_implicit_invocation: true
