interface:
  display_name: "Install Axon"
  short_description: "Install, secure, and verify Axon"
  default_prompt: "Use $install-axon to install or repair Axon and connect my agent safely."
policy:
  allow_implicit_invocation: false
